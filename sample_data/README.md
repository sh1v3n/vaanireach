# sample_data/

Small, non-sensitive, **synthetic** sample documents for local testing of
the (future) ingestion pipeline. Nothing here is a real government notice
or references any real person, scheme, or organization.

- `sample_scheme_notice.txt` — a short fake scheme announcement usable as
  a fixture input once `DocumentParser` / `FactExtractor` are implemented
  in Phase 1.
