# tests/

Phase 0 tests only assert import correctness and stub contracts — no
pipeline behavior is tested yet, because no pipeline behavior exists yet.

Run with:

```bash
cd backend && source .venv/bin/activate
PYTHONPATH=".:.." pytest ../tests
```
