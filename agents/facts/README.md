# Fact Extraction Agent

Runs one or more `FactExtractor`s over parsed document pages and assembles
the Source Fact Ledger for a project, including assigning `criticality`.

Will implement/consume: [`core.interfaces.fact_extractor.FactExtractor`](../../core/interfaces/fact_extractor.py)

No logic implemented in Phase 0 — this package only reserves the import
namespace for Phase 1.
