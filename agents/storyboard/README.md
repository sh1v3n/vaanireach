# Storyboard Agent

Runs the `SceneDirector` over a verified Script to produce a Storyboard —
this is the genuine per-fact decision point (amount → animated number,
location → map, deadline → calendar animation, statistic → chart, …).

Will implement/consume: [`core.interfaces.scene_director.SceneDirector`](../../core/interfaces/scene_director.py)

No logic implemented in Phase 0 — this package only reserves the import
namespace for Phase 1.
