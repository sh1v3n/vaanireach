# Copy this file to .env and fill in real values locally.
# NEVER commit .env — it is already excluded via .gitignore.

ENV=development

# --- Storage ---
DATABASE_URL=sqlite:///./vaanireach.db
UPLOAD_DIR=./data/uploads

# --- Upload / file safety limits ---
MAX_UPLOAD_SIZE_MB=25
ALLOWED_FILE_TYPES=pdf,docx,png,jpg,jpeg

# --- Web ---
CORS_ORIGINS=http://localhost:3000

# --- Provider timeouts (seconds) ---
PROVIDER_TIMEOUT_SECONDS=30

# --- Provider API keys (all optional in Phase 0 — no provider is selected yet) ---
# LLM_API_KEY=
# TRANSLATION_API_KEY=
# TTS_API_KEY=
# VISUAL_PROVIDER_API_KEY=
# VIDEO_PROVIDER_API_KEY=
